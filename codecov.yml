coverage:
  status:
    project:
      default:
        target: auto
        # Coverage can drop by 5% and still be a success
        threshold: 5%
      cosmos-ruby:
        paths:
          - cosmos
      frontend:
        paths:
          - cosmos-init
      api:
        paths:
          - cosmos-cmd-tlm-api
          - cosmos-script-runner-api
